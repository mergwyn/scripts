kopiacmd:
- --help
- --version
- repo
- system
- install
- users
- policy
- completion

kopiacmd repo:
- create
- connect
- list
- valid
kopiacmd repo create:
- TODO
kopiacmd repo connect:
- $(ls ~/.config/kopia/*.config | sed -e 's:.*/::' -e 's/.config$//')
kopiacmd repo valid:
- kopia
- veloro
kopiacmd repo valid velero:
- $(kopiacmd repo valid velero)
kopiacmd repo valid kopia:
- $(kopiacmd repo valid kopia)

kopiacmd system:
- install
- certs
kopiacmd system install:
- $(ls ~/.config/kopia/*.config | sed -e 's:.*/::' -e 's/.config$//')
kopiacmd system certs:
- $(ls ~/.config/kopia/*.config | sed -e 's:.*/::' -e 's/.config$//')

kopiacmd users:
- add
kopiacmd users add:
- $(ls ~/.config/kopia/*.config | sed -e 's:.*/::' -e 's/.config$//')

kopiacmd policy:
- global
kopiacmd policy global:
- $(ls ~/.config/kopia/*.config | sed -e 's:.*/::' -e 's/.config$//')

kopiacmd completion:
- TODO
